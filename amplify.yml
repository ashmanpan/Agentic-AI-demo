version: 1
frontend:
  phases:
    # IMPORTANT: Amplify doesn't have a build phase for static HTML
    # We just need to specify what files to deploy
    build:
      commands:
        - echo "No build required for static HTML"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []